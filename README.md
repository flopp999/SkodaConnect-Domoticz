# SkodaConnect-Domoticz

to download this plugin:

In domoticz/plugins run command "sudo git clone https://github.com/flopp999/SkodaConnect-Domoticz.git"
Then run "sudo pip install -r requirements.txt"
